#!/bin/bash
# cluster-fork COMMANDS
# Script to execute COMMANDS in all nodes in /etc/machines
# Juan Caballero @ Cinvestav 2008

for NODE in node{20..29}
do
    echo $NODE:
    ssh $NODE "nova-manage service disable $NODE nova-compute"
done
